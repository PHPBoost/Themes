<input
    type="password"
    name="${escape(NAME)}"
    id="${escape(HTML_ID)}"
    value="${escape(VALUE)}"
    class="form-control ${escape(CLASS)}"
    # IF C_DISABLED # disabled="disabled" # ENDIF # />
