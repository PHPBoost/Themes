jQuery(document).ready(() => { jQuery(window).scroll(() => { let e = jQuery("#header").outerHeight(); jQuery(window).scrollTop() > e ? jQuery("#header-container").addClass("fixed-header") : jQuery("#header-container").removeClass("fixed-header") }) }), jQuery("#menus-trigger").on("click", () => { jQuery("#menus-trigger").toggleClass("fa-times error") }), jQuery("#menus-trigger").opensubmenu({ osmTarget: "#header-container", osmCloseExcept: "#header-container *", osmClass: "active-menus" }); let headerHeight = jQuery("#site-name").outerHeight(); jQuery(".sticky-menu").css("top", headerHeight + "px");